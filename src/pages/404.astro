---
import Layout from '@/layouts/Layout.astro';
import { getPageInfo } from '@/lib/wp';

const { title, content } = await getPageInfo('404-2');
---

<Layout title={title} description={content}>
  <div class="pb-10 flex items-center justify-center px-4">
    <div class="max-w-6xl mx-auto">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        
        <!-- Donut Chart with 404 -->
        <div class="relative flex items-center justify-center">
          <div class="absolute left-0 text-9xl lg:text-[12rem] font-black text-white opacity-20 select-none">
            4
          </div>
          <!-- SVG Donut Chart -->
          <div class="relative z-10">
            <svg class="w-60 h-60 lg:w-80 lg:h-80" viewBox="0 0 200 200">
              <!-- Background circle -->
              <circle
                cx="100"
                cy="100"
                r="80"
                fill="none"
                stroke="#1f2937"
                stroke-width="20"
              />
              
              <!-- Large segment (99.5%) -->
              <circle
                cx="100"
                cy="100"
                r="80"
                fill="none"
                stroke="#2847d7"
                stroke-width="20"
                stroke-dasharray="499.6 1.4"
                stroke-dashoffset="125"
                transform="rotate(-90 100 100)"
                class="animate-pulse"
              />
              
              <!-- Center text -->
              <text
                x="100"
                y="90"
                text-anchor="middle"
                class="fill-white text-lg font-bold"
                font-family="system-ui"
              >
                Página
              </text>
              <text
                x="100"
                y="110"
                text-anchor="middle"
                class="fill-white text-lg font-bold"
                font-family="system-ui"
              >
                no encontrada
              </text>
            </svg>
          </div>
          
          <!-- Large "4" on the right -->
          <div class="absolute right-0 text-9xl lg:text-[12rem] font-black text-white opacity-20 select-none">
            4
          </div>
        </div>
        
        <!-- Message and Stats -->
        <div class="space-y-8 text-center lg:text-left">
          <!-- Main Message -->
          <div class="space-y-4">
            <h1 class="text-4xl lg:text-6xl font-black text-white">
              ¡Ups!
            </h1>
            <p class="text-xl lg:text-2xl text-gray-300">
              Parece que te has perdido en el ciberespacio
            </p>
            <p class="text-gray-400 text-lg">
              La página que buscas no existe o ha sido movida a otra dimensión digital.
            </p>
          </div>
          
          <!-- Statistics -->
          <div class="space-y-4 p-6 bg-black-700 rounded-xl border border-[#2847d7]">
            <h3 class="text-lg font-semibold text-white mb-4">Estadísticas curiosas:</h3>
        
            <div class="flex items-center space-x-3">
              <span class="text-gray-300">
                <span class="font-bold text-white">99.5%</span> de los visitantes nunca ven esta página
              </span>
            </div>
            
            <div class="flex items-center space-x-3">
              <span class="text-gray-300">
                <span class="font-bold text-white">0.5%</span> la descubren por accidente
              </span>
            </div>
            
            <!-- Congratulations message -->
            <div class="mt-4 p-3 bg-gray-800 rounded-lg border-l-4 border-[#2847d7]">
              <p class="text-sm text-gray-300">
                🎉 <span class="text-white font-semibold">¡Felicidades!</span> 
                Eres parte del selecto grupo del 0.5%
              </p>
            </div>
          </div>
          
          <!-- Action Buttons -->
          <div class="space-y-4 lg:space-y-0 lg:space-x-4 lg:flex">
            <a 
              href="/" 
              class="inline-flex items-center justify-center px-6 py-3 bg-[#2847d7] hover:bg-blue-600 text-white font-semibold rounded-lg transition-colors duration-300 group w-full lg:w-auto"
            >
              <svg class="w-5 h-5 mr-2 group-hover:-translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
              </svg>
              Volver al inicio
            </a>
            
            <a 
              href="/posts" 
              class="inline-flex items-center justify-center px-6 py-3 bg-gray-800 hover:bg-gray-700 text-white font-semibold rounded-lg transition-colors duration-300 border border-gray-700 w-full lg:w-auto"
            >
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
              </svg>
              Ver artículos
            </a>
          </div>
          
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* Custom animation for the donut chart */
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.8;
    }
  }
  
  .animate-pulse {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  
  /* Prevent text selection on decorative elements */
  .select-none {
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
  }
</style>